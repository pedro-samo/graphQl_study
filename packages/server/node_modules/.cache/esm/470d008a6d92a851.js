let express;_6d9‍.w("express",[["default",["express"],function(v){express=v}]]);

const server = express();

server.get(
  "/status",
  (_,
  (response) => {
    response.send({
      status: "Okay",
    });
  })
);

server.post("/authenticate", express.json(), (request, response) => {
  _6d9‍.g.console.log("E-mail", request.body.email, "Senha", request.body.password);
  response.send();
});

const PORT = process.env.PORT ? parseInt(process.env.PORT) : 8000;
const HOSTNAME = process.env.HOSTNAME || "127.0.0.1";

server.listen(PORT, HOSTNAME, () => {
  console.log(`Server is listing at http://${HOSTNAME}:${PORT}`);
});
